https://github.com/Light-City/CPlusPlusThings/tree/master/basic_content/struct_class

总的来说，struct更适合看成一个数据结构的实现体，而class更适合看成是一个对象的实现体

区别：
最本质的区别是默认的访问控制
默认的继承访问权限。struct是public,class是private


类的成员默认是private,结构体的成员默认是public。
struct作为数据结构的实现体，默认的数据访问控制是public;而class作为对象的实现体，默认的成员变量访问控制是private类型