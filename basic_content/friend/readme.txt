注意：
友元关系没有继承性：假如B是A的友元类，类C继承于A，则B与C不存在友元关系
友元关系没有传递性：假如B是A的友元类，C是B的友元类，则C不是A的友元类

在类外调用类的私有成员函数：
通过friend定义一个类的友元函数fun2，然后在fun2里面调用类的私有成员函数fun1
NOTE::前提是fun1必须定义为static类型，这样可以在友元函数里面通过类名调用，不定义static类型的话，报错没有具体对象调用私有函数