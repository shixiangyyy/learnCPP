基类与派生类对象的关系：
基类对象与派生类对象之间存在赋值相容性

包括以下几种情况：
1、把派生类对象赋值给基类对象(反之不行，上行没问题，下行存疑，参考dynamic_cast,进行RTTI运行时类型检测)；
2、把派生类对象的地址赋值给基类指针(Base* bptr = new Derived();,反之不行，不能用基类对象的地址赋值派生类指针);
3、用派生类对象初始化基类对象的引用(反之不行)。